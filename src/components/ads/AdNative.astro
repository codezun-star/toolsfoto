---
/**
 * Banner nativo de Adsterra (se integra con el estilo del sitio).
 *
 * IMPORTANTE: el contenedor lleva un id fijo impuesto por la red, así que
 * solo puede haber UN banner nativo por página. No incluir este componente
 * dos veces en la misma plantilla.
 *
 * El script se inyecta desde el loader global cuando el contenedor se acerca
 * al viewport, igual que el resto de huecos.
 */
---

<div class="tf-ad tf-ad-native" data-ad="native">
  <div id="container-934ea82384654687f34f72b2e6d95efc"></div>
</div>
